# Inventory file for BIND9 DNS servers
# Define your DNS servers here

[dns_servers]
# Primary DNS server
primary-dns ansible_host=172.30.0.53 ansible_user=root

# Secondary DNS server  
secondary-dns ansible_host=172.30.0.56 ansible_user=root

# You can also define groups for primary and secondary separately if needed
[primary_dns]
primary-dns

[secondary_dns]
secondary-dns